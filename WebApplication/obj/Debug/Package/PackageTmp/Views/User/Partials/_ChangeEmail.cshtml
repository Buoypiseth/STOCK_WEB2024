@model WebApplication.Models.User
@using (Html.BeginForm("ChangeEmail", "User", FormMethod.Post))
{
    <div class="modal fade" id="modal-email">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 id="modal-title" class="modal-title text-secondary">
                        <i class="fa fa-edit"></i>
                        @Resources.Globle.Edit
                    </h5>
                    <div class="float-right">
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                </div>
                <div class="modal-body">
                    <div class="form-horizontal">
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        <div class="form-group">
                            @Html.Hidden("txtEmailId", "", new { @class = "form-control", @placeholder = "id" })
                            @Html.Label(Resources.User.Email, htmlAttributes: new { @class = "control-label" })
                            @Html.TextBox("txtEmail", "", new { @class = "form-control", @type = "email", @required = "required", @placeholder = Resources.User.Email })
                            @Html.ValidationMessageFor(model => model.Email, "", new { @class = "text-danger" })
                        </div>
                    </div>
                </div>
                <div class="modal-footer justify-content-between">
                    <button type="button" class="btn btn-danger btn-min" data-dismiss="modal">
                        <i class="fa fa-times-circle" aria-hidden="true"></i>
                        @Resources.Globle.Cancel
                    </button>
                    <button type="submit" id="btnSaveOrUpdate" class="btn btn-success btn-min">
                        <i class="far fa-check-circle"></i>
                        @Resources.Globle.Save
                    </button>
                </div>
            </div>
            <!-- /.modal-content -->
        </div>
        <!-- /.modal-dialog -->
    </div>
    <!-- /.modal -->
}
